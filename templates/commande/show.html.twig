{% extends 'base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>DateHeureDepart</th>
                <td>{{ commande.dateHeureDepart ? commande.dateHeureDepart|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DateHeureFin</th>
                <td>{{ commande.dateHeureFin ? commande.dateHeureFin|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>PrixTotal</th>
                <td>{{ commande.prixTotal }}</td>
            </tr>
            <tr>
                <th>DateEnregistrement</th>
                <td>{{ commande.dateEnregistrement ? commande.dateEnregistrement|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande_index') }}" class="btn btn-dark mt-3">Retour à la liste des commandes</a>

    <a href="{{ path('app_commande_edit', {'id': commande.id}) }}" class="btn btn-dark mt-3">Modifier</a>

    {{ include('commande/_delete_form.html.twig') }}
{% endblock %}
